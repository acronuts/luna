const baseUrl = "sagittarius-luna.propulsion-learn.ch"

export const USER_LOGIN = "USER_LOGIN";

export const authentication = `${baseUrl}/api/auth/token/`;

export const registration = `${baseUrl}/api/registration/`

export const validation = `${baseUrl}/api/registration/validate/`

export const USER_TOKEN = "USER_TOKEN"